spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/FilmSite
    username: user
    password: passuser
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  jackson:
    serialization:
      indent_output: true

server:
  port: 8080
